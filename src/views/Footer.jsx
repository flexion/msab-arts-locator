import React from 'react';
import { Container, Section, Footer } from 'bloomer';

export const Foot = () => (
  <Footer className="msab-has-background-teal msab-footer">
    <Section className="msab-footer">
      <Container>Presented by the Minnesota State Arts Board</Container>
    </Section>
  </Footer>
);
